[project]
name = "ecdat-api"
version = "0.1.0"
description = "API and cryptographic discovery engine for ECDAT"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
    "uvicorn[standard]>=0.30,<1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<9.0",
    "httpx>=0.27,<1.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
# Use pytest's isolated per-run temporary directory. A fixed workspace folder
# can remain locked on Windows after a run and make subsequent tests fail.
addopts = "-p no:cacheprovider"
